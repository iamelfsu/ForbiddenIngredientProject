using UnityEngine;
using UnityEngine.UI;

public class CookingBar : MonoBehaviour
{
    /*public Slider cookingBar;

    private int maxCookingTime = 25;
    private int currentCookingTime;
    public CookTimer cookTimer;

    public static CookingBar Instance;
    public Pan Pan; 

    void Awake()
    {
        Instance = this;
    }

    void Start()
    {
        cookTimer.currentTime = -cookingBar.value;
        currentCookingTime = maxCookingTime;
        cookingBar.maxValue = maxCookingTime;
    }


    public void UseCookingTime(int amount)
    {
        if(currentCookingTime - amount >= 0)
        {
            currentCookingTime -= amount;
            cookingBar.value = currentCookingTime;
        }
        else
        {
            currentCookingTime = 0;
            cookingBar.value = currentCookingTime;
            Debug.Log("Cooked!");
            Pan.CookedM();
        }
    }*/
}
